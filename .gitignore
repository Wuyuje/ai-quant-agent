node_modules/
.env
config/greenfield.json

# ── 敏感数据（含 API Key、密码、session、交易记录）──
data/
!data/.gitkeep
!data/databus.js

# ── 备份（可能含旧密钥）──
backup/

# ── 日志 ──
logs/
*.log

# ── 临时SSH连接测试工具(含敏感服务器连接逻辑, 不入库) ──
_sshtest*.js
secure-conn.js

# Supervisor chat inbox (运行时数据)
supervisor/
